Cuando se envía una petición POST a la ruta /registro con un JSON,
el servidor responde con un objeto JSON que confirma la recepción de los datos.
La respuesta incluye el estado "Datos recibidos", junto con el nombre y el mensaje enviados.